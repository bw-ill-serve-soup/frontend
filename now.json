{
    "version": "0.1.0",
    "name": "iss-frontend",
    "builds": [{ "src": "iss-frontend/package.json", "use": "@now/static-build", "config": {"distDir": "build"} }],
    "routes": [
        { "src": "/(.*)", "dest": "iss-frontend/$1" }
    ]
}